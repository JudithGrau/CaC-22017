/* let num1 = Number(prompt ("ingresa un numero"));

let num2= Number(prompt ("ingresa otro numero"));

let resultado = num1 + num2;

document.write (`el resultado de su suma es ${resultado}`); */


// IF (Condicionales)
// pedir el nombre; 
// pedir la edad;
// si tiene 18 años devolver un alert ( nombre + "sos mayor") , sino (nombre + "sos menor")
/* let nombre = prompt ("ingresa tu nombre");
let edad = Number (prompt(` ${nombre} por favor ingresa tu edad`)); */

/*  if (edad >= 18){
    alert (`${nombre} sos mayor`)
}else {
    alert (`${nombre} sos menor`)
}
  */
/*  if (edad >= 18){
    alert (`${nombre} sos mayor`)
} else if (edad<=18 ){
    alert (`${nombre} sos menor`)
}else {
    alert ("error");
} */
 

//Ejercicio 2
// Crear un programa que:
// pida el nombre
// mostrar un alerta que le diga la cantidad de letras que tiene su nombre al usuario/a
// que pida un numero
// mostrar un alerta al usuario/a que indique suma del numero ingresado + la cantidad de caracteres de su nombre.

  

/* let nombre = prompt ("ingresa tu nombre");

document.write (`${nombre} tu nombre tiene ${nombre.length} letras`) */
